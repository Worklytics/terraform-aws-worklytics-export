# variables just for examples
# as you're likely adding the `terraform-aws-worklytics-export` module to existing config, you won't
# need equivalent in your usage

variable "aws_account_id" {
  type = string
}

variable "aws_role_name" {
  type = string
}
